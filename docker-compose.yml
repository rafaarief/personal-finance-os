services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pfos
      POSTGRES_PASSWORD: pfos
      POSTGRES_DB: pfos
    ports:
      - "5432:5432"
    volumes:
      - pfos_postgres:/var/lib/postgresql/data

volumes:
  pfos_postgres:
